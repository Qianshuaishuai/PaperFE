import { TypeToTypePipe } from './type-to-type.pipe';

describe('TypeToTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeToTypePipe();
    expect(pipe).toBeTruthy();
  });
});
