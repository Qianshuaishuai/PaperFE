import { CountToArrayPipe } from './count-to-array.pipe';

describe('CountToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new CountToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
